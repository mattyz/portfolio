<div class="container">
  <div class="row">
    <div class="col-md-7">
      <%= image_tag @portmanteau.main_image %>
    </div>
    <div class="col-md-5">
      <h1><%= @portmanteau.title %></h1>
      <hr>
      <em><%= @portmanteau.subtitle %> </em>

      <p><%= @portmanteau.body%> </p>

      <hr>

      <h3>Technologies Used </h3>
      <% @portmanteau.technologies.each do |t| %>
        <p> <%= t.name %> </p>
      <% end %>

    </div>
  </div>
</div>

<%#= params.inspect %>
<%#= session.inspect %>
